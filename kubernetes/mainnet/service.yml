apiVersion: v1
kind: Service
metadata:
  name: api-casperholders-service
spec:
  ports:
    - name: web
      port: 80
      targetPort: 80
  selector:
    app: casperholders-api
status:
  loadBalancer: {}